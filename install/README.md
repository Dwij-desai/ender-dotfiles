# Install Scaffold

This directory holds installer-related architecture and implementation scaffolding.

Current status:

- Installer interface is intentionally pending design review.
- Root [install.sh](../install.sh) remains reserved for the future stable install path.

Planned contents:

- package group manifests
- symlink mapping definitions
- service policy references
- migration notes
